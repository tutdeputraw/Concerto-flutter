part of '../init.dart';

class DetailUpcomingConcert extends StatelessWidget {
  const DetailUpcomingConcert({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
