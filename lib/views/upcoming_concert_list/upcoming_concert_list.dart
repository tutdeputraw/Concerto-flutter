part of '../init.dart';

class UpcomingConcertList extends StatelessWidget {
  const UpcomingConcertList({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
