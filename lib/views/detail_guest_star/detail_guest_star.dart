part of '../init.dart';

class DetailGuestStar extends StatelessWidget {
  const DetailGuestStar({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
